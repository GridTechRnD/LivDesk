import availabilityRoutes from 'dashboard/routes/dashboard/settings/availability/availability.routes.js';

const adminRoutes = {
  routes: [
    ...availabilityRoutes.routes,
  ],
};

export default adminRoutes;
